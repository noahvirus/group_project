<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="/"></a>
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <!-- TO-DO: For 'Home', Add a <a> tag with an attribute href to call the '/' API that navigates to home page if logged in, or login page if not -->
            <a class="nav-link" href = "/home">Home</a>
        </li>
        <li class="nav-item">
          <!-- TO-DO: For 'My Courses', add a <a> tag with an attribute href to call the '/courses?taken=true' API that navigates to courses page, showing the courses the student has taken, if logged in, or login page if not -->
            <a class="nav-link" href = "/discover" > Discover </a>
        </li>
        <li class="nav-item">
          <!-- TO-DO: For 'My Courses', add a <a> tag with an attribute href to call the '/courses?taken=true' API that navigates to courses page, showing the courses the student has taken, if logged in, or login page if not -->
            <a class="nav-link" href = "/travel" > Travel </a>
        </li>
          <!-- TO-DO: For 'Courses', add a <a> tag with an attribute href to call the '/courses' API that navigates to courses page if logged in, or login page if not -->
            <a class="nav-link" href = "/profile"> Profile </a>
        </li>
        <li class="nav-item">
          <!-- TO-DO: For 'Log out', add a <a> tag with an attribute href to call the '/logout' API that navigates to the logout page -->
            <a class="nav-link" href = "/login"> Log In </a>
        </li>
        <li class="nav-item">
          <!-- TO-DO: For 'Log out', add a <a> tag with an attribute href to call the '/logout' API that navigates to the logout page -->
            <a class="nav-link" href = "/register"> Register </a>
        </li>
      </ul>
    </nav>
  </header>
  <style>
    body, html {
  height: 100%;
  }
  
  * {
  box-sizing:border-box;
  
  }
  
  .bg-image {
  /* The image used */
  background-image: url("https://media.giphy.com/media/Mgq7EMQUrhcvC/giphy.gif");
  
  /* Add the blur effect */
  filter: blur(2px);
  -webkit-filter: blur(2px);
  
  /* Full height */
  height: 100%;
  
  /* Center and scale the image nicely */
  background-position: center;
  background-repeat: no-repeat;
  background-size: cover;
  }
  
  /* Position text in the middle of the page/image */
  .bg-text {
  background-color: rgb(0,0,0); /* Fallback color */
  background-color: rgba(0,0,0, 0.4); /* Black w/opacity/see-through */
  color: white;
  font-weight: bold;
  border: 0px solid #f1f1f1;
  position: absolute;
  top: 57%;
  left: 50%;
  transform: translate(-50%, -50%);
  z-index: 2;
  width: 100%;
  height: 100%;
  padding: 20px;
  text-align: center;
  }
  </style>

<% if (locals.message) { %>

  <style>
      .bg-text {
  
  /* Full height */
  position:relative; top:-367px;
  

  }
  </style>


<% } %>


  <div class="bg-image"></div>
  <div class="bg-text">